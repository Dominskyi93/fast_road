package com.chicken.fast.road.navigation

enum class Route(
    val route: String
) {
    LOADING("loading"),
    HOME("home"),
    GAME("game"),
    RECORDS("records"),
    CUSTOM("custom"),
    SETTINGS("settings")
}